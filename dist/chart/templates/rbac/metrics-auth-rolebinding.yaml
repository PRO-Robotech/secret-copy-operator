{{- if .Values.metrics.enable }}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
    name: secret-copy-operator-metrics-auth-rolebinding
roleRef:
    apiGroup: rbac.authorization.k8s.io
    kind: ClusterRole
    name: secret-copy-operator-metrics-auth-role
subjects:
    - kind: ServiceAccount
      name: secret-copy-operator-controller-manager
      namespace: {{ .Release.Namespace }}
{{- end }}
