apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
    name: secret-copy-operator-manager-role
rules:
    - apiGroups:
        - ""
      resources:
        - events
      verbs:
        - create
        - patch
    - apiGroups:
        - ""
      resources:
        - secrets
      verbs:
        - create
        - get
        - list
        - patch
        - update
        - watch
